{
  "walls": {},
  "small_tiles": false,
  "goal": {
    "position": {
      "image": "racing_flag",
      "x": 1,
      "y": 1
    }
  },
  "onload": "function getRandomInt(min, max) { return Math.floor(Math.random() * (max - min + 1) + min);} var height, width, x, y; width = getRandomInt(3, 12); height = getRandomInt(3, 12); RUR.set_world_size(width, height); RUR.fill_background(\"pale_grass\"); x = getRandomInt(1, width); y = getRandomInt(1, height); r = new UsedRobot(x, y); RUR.set_random_orientation();"
}